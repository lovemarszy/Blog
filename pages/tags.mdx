---
type: page
title: Tags
date: 2023-02-06
---

# Tags

这里是我所有标签的索引。

export const TagList = () => {
  // 🛠️ 在这里管理你的标签和简介
  // 注意：name 必须和你文章里写的 tags 完全一致
  const tags = [
    {
      name: "坐下聊聊",
      desc: "我的一些思考和想法"
    },
    {
      name: "奇思妙想",
      desc: "一个一个梦飞出了天窗"
    },
    {
      name: "小玩具",
      desc: "我买过的一些有意思的小玩意"
    },
    {
      name: "书影音",
      desc: "多看，多读，多听"
    },
  ]

return (
<div className="flex flex-col gap-6 mt-8">
{tags.map((tag) => (
<div 
          key={tag.name} 
          className="flex flex-col sm:flex-row sm:items-baseline gap-1 sm:gap-4 border-b border-dashed border-gray-200 dark:border-gray-800 pb-4 last:border-0"
        >
{/* 左侧：标签链接 */}
<a
href={`/tags/${tag.name}`}
className="text-lg font-medium text-gray-900 dark:text-gray-100 underline decoration-2 underline-offset-4 decoration-gray-300 hover:decoration-gray-600 transition-all shrink-0" >
{tag.name}
</a>

          {/* 右侧：简介 */}
          <span className="text-gray-600 dark:text-gray-400 text-sm sm:text-base">
            {tag.desc}
          </span>
        </div>
      ))}
    </div>

)
}

<TagList />
